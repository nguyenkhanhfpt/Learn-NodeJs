h2 Create a new person 

form(action="/phonebook/add", method="POST")
    lable(for="name") Name
    input#name.form-control(type="text" name="name")
    lable(for="phone") Phone number
    input#phone.form-control(type="text" name="phone")
    button Add

